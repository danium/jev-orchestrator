# Jev Orchestrator Codex plugin

This plugin bundles the Jev Orchestrator skill and runtime. The skill keeps
live execution opt-in and directs normal use through read-only diagnosis and
offline routing first.

The runtime is in runtime/. It uses the installed Codex App Server schema
snapshot and has no runtime npm dependencies.
